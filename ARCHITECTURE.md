# Architecture

Placeholder. This repo isn't wired into [nixie](https://github.com/amatos/nixie)
yet — the home-manager migration described in `CLAUDE.md` ("What this is",
"Layout") hasn't happened. Once it has, this document should explain how
nixie-homes and nixie connect (standalone vs. integrated use, the one-way
dependency direction, the `homeModules`/`homeConfigurations` flake outputs),
mirroring nixie's own `ARCHITECTURE.md`. Until then, see nixie's
`ARCHITECTURE.md` for the current cross-repo picture (it does not yet mention
this repo).
